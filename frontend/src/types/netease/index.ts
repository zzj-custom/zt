type NeList = {
  flag: string;
  name: string;
  musicId: string;
  musicName: string;
  albumPic: string;
  duration: number;
  format: string;
  status: number;
};

type SelectFolderList = {
  title: string;
  value: number;
};

export type { NeList, SelectFolderList };
